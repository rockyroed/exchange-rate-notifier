[tool.black]
line-length = 120

[tool.isort]
profile = "black"
line_length = 120
include_trailing_comma = true
multi_line_output = 3
skip_glob = [".venv/*"]

[tool.codespell]
skip = "./venv,./.venv"

[tool.ruff]
target-version = "py313"
